package com.sist.movie; // 현재 폴더 위치
import com.sist.board.*;// 다른 패키지에 있는 클래스 사용
// 라이브러리도 import
/* 
 	import 생략가능한 것 : import java.lang.* (String, Math, System)
 */
public class MovieList {
	BoardList b = new BoardList();
	
	void display() {
		
	}
}
